export function Questions() {
  return (
    <section
      className="space-y-4"
      style={{ color: "rgb(var(--color-text-primary))" }}
    >
      <h1>Questions</h1>
      <p style={{ color: "rgb(var(--color-text-secondary))" }}>
        Question management tools will appear here in a future update.
      </p>
    </section>
  );
}
