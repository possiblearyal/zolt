export function Settings() {
  return (
    <section
      className="space-y-4"
      style={{ color: "rgb(var(--color-text-primary))" }}
    >
      <h1>Settings</h1>
      <p style={{ color: "rgb(var(--color-text-secondary))" }}>
        Configure system preferences from this placeholder view.
      </p>
    </section>
  );
}
