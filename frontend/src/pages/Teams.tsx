export function Teams() {
  return (
    <section
      className="space-y-4"
      style={{ color: "rgb(var(--color-text-primary))" }}
    >
      <h1>Teams</h1>
      <p style={{ color: "rgb(var(--color-text-secondary))" }}>
        Track and configure teams from this placeholder page.
      </p>
    </section>
  );
}
